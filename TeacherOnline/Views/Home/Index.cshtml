@{
    ViewData["Title"] = "Home Page";
}
@model IEnumerable<TeacherOnline.DAL.Entities.User>

    <h1>Дипломная работа</h1>
    <h1>Пока так, придумать наполнение (приветствие) @User.Identity.IsAuthenticated</h1>
    <h1>User.IsInRole == @User.IsInRole("Teacher")</h1>

@*<p>
    <label>Аватар</label>
    <input name="Avatar" type="file" class="form-control" />
</p>
<p>
    <input type="submit" value="Добавить" />
</p>
*@
@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <p>
        Список пользователей
    </p>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Логин</th>
                <th scope="col">Пароль</th>
                <th scope="col">Ранг</th>
                <th scope="col">Почта</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td>@item.Login</td>
                    <td>@item.Password</td>
                    <td>@item.Rank</td>
                    <td>@item.Email</td>
                    <td><a class="btn btn-warning" asp-controller="Users" asp-action="UpdateUser" asp-route-id="@item.Id">Изменить</a></td> @*тут изменить на "изменить"*@
                    <td>
                        <form method="post" asp-controller="Users" asp-action="DeleteUser" asp-route-id="@item.Id">
                            <button class="btn btn-danger" type="submit">Удалить</button>
                    </form> </td> 
                </tr>
            }
        </tbody>
    </table>
}