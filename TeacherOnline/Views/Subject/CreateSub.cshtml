@{
    ViewData["Title"] = "Создать предмет";

    //public record class Teach(int id, string fio);
    //static List<Teach> teahc { get; } = new List<Teach>();
    //foreach(var item in )
    //SelectList selects = new SelectList()
}
@*@model TeacherOnline.DAL.Entities.Subject;*@
@model TeacherOnline.DTO.ViewModel.SubjectGroupTeacherVM;
<h2>@ViewData["Title"]</h2>

<form asp-controller="Subject" asp-action="CreateSub" method="post">
    <div class="mb-3">
        <label class="form-label">Название предмета</label>
        <input type="text" class="form-control" asp-for="@Model.sub.Name">
        <div class="col-md-3">
            <label for="inputDepartment" class="form-label">Группа</label>
            <select class="form-select" asp-for="@Model.sub.IdTeacterPract">
                @foreach (var item in Model.teacher)
                {
                    <option value="@item.Id">@item.FirstName @item.LastName @item.Otchestvo</option>
                }
            </select>
        </div>
        <label class="form-label">Номер подгруппы для доп. преподавателя</label>
        <input type="range" class="form-range" min="0" max="1" asp-for="@Model.sub.PartGroup">
        <label class="form-label">О предмете</label>
        <input type="text" class="form-control" asp-for="@Model.sub.About">
    </div>
    <button type="submit" class="btn btn-primary">Создать предмет</button>
</form>